Команды на один раз для устройства ПК/Ноут
git config --global user.name "Your name"
git config --global user.email "your-email@example.com"

Команды на один раз для проекта
git init
git remote add origin https://github.com/Svetlana-geeks/OOP-50-02.git

Команды, которые часто пишутся в проекте
git add .    | Команда копирует и сохраняет в буфер (невидимый)
git commit -m  | Команда сохраняет изменения в файле .git
git push origin master  | Команда отправляет изменения в репозиторий и сохраняет
git branch  | Команда для просмотра списка веток
git status  | Команда для просмотра состояния проекта или изменений